package ${packageName};

//domain use case
public class ${useCaseName} {
	
	private final ${modelName}Repository repository;

	public ${useCaseName} (${modelName}Repository repository) {
		this.repository = repository;
	} 

}